{
  "name": "aiera-client-sdk",
  "version": "0.0.1",
  "description": "The Aiera Client SDK",
  "main": "index.js",
  "scripts": {
    "build:watch:docs": "docz dev --typescript --port 3003",
    "build:docs": "docz build --typescript",
    "build:lib": "lerna run build",
    "build:watch:lib": "lerna run --parallel build:watch",
    "build": "concurrently npm:build:*",
    "build:watch": "concurrently npm:build:watch:*",
    "lerna": "lerna",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Aiera, Inc.",
  "license": "CC-BY-NC-ND-4.0",
  "devDependencies": {
    "concurrently": "^6.2.0",
    "docz": "^2.3.1",
    "lerna": "4.0.0",
    "react": "17.0.2",
    "react-dom": "17.0.2"
  },
  "dependencies": {}
}
