import React from 'react';
import { screen } from '@testing-library/react';

import { renderWithClient } from 'testUtils';
import { {{name}}, {{name}}UI } from '.';

describe('{{name}}', () => {
    test('renders', () => {
        renderWithClient(<{{name}} />);
        screen.getByText('{{name}}UI');
    });
});
